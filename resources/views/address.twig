{% extends layout('checkout') %}

{% block content %}
    <section id="checkout">

        <h2>Checkout</h2>

        <br><br>

        {{ form_get('checkout.customer')
        .entry(checkout.cart_id)
        .successMessage(false)
        .redirect('checkout/shipping').get()|raw }}

    </section>
{% endblock %}
