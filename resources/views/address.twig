{% extends layout('checkout') %}

{% block content %}
    <section id="checkout">

        <h2>Checkout</h2>

        <br><br>

        {{ form('checkout.customer')
        .entry(checkout.cart)
        .successMessage(false)
        .redirect('checkout/shipping').get()|raw }}

    </section>
{% endblock %}
